# Vault Report Generators

This folder contains generators that report on the Obsidian vault itself.

## Vault Health

Generate a vault-health dashboard:

```bash
node user-support/kpi-reports/scripts/report-generators/vault/generate_vault_health.js
```

The output package is written to `user-support/kpi-reports/vault-health-demo/`.
